CREATE TABLE user (id int primary key auto_increment, firstname varchar(40), lastname varchar(40), email varchar(20), password varchar(40));

CREATE TABLE product(id int primary key auto_increment, title varchar(100),price float);